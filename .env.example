# Copy to .env.local and fill in. .env.local is gitignored — never commit a real key.
ANTHROPIC_API_KEY=sk-ant-...

# The HR gate. Access control for a demo, not authentication — one shared
# passcode, no accounts. Defaults to hr / letmein when unset.
HR_ID=hr
HR_PASSCODE=letmein
